language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
install:
  - "pip install -U setuptools"
  - "python setup.py install"
  - "pip install -U pytest coverage python-coveralls"
script: "coverage run --source busbus -m pytest"
after_script:
  - "coverage report -m"
  - "coveralls"
